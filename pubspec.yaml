name: og_networking
description: OG Networking Study Material App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0

  file_picker: ^8.0.0
  flutter_pdfview: ^1.3.2
  path_provider: ^2.1.3
  http: ^1.2.2

  video_player: ^2.9.1
  chewie: ^1.8.5

flutter:
  uses-material-design: true
